export default (
  messages: Array<QueryMessagesFeedMessage>
): Array<QueryMessagesFeedMessage> => {
  const messageIds = new Set<string>();
  const newMessages: Array<QueryMessagesFeedMessage> = [];

  messages.forEach((message) => {
    const messageKey;
  });
};
