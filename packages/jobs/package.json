{
  "name": "@disappearing-messages/jobs",
  "version": "0.0.1",
  "scripts": {
    "check": "npx npm-check --skip-unused",
    "delta-lint:code": "eslint $(git diff --name-only --diff-filter=ACMRTUXB origin/main | grep -E 'packages/jobs/.*(.js$|.ts$|.tsx$)' | sed -e 's:packages/[^/]*/::g')",
    "lint:code": "eslint .",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "@disappearing-messages/common": "file:../common",
    "@disappearing-messages/domain": "file:../domain"
  },
  "devDependencies": {}
}
